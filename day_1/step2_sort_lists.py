# Definition: sort lists loaded in previous step.
#   Assume happy path: numbers only, non-negative

# Sample input (alredy loaded)
left_list = [3, 4, 2, 1, 3, 3]
right_list = [4, 3, 5, 3, 9, 3]

left_list.sort()
right_list.sort()

print(left_list)
print(right_list)
